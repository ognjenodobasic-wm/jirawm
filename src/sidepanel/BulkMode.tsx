export default function BulkMode() {
  return (
    <div className="p-3">
      <p className="text-sm text-[var(--chrome-text-secondary)]">
        Bulk mode — queue multiple screenshots and create Jira subtasks in one batch.
      </p>
    </div>
  );
}
